services:
  python-build:
     build:
      context: .
      dockerfile: Dockerfile
      args:
        - PYTHON_VERSION=${PYTHON_VERSION}
     volumes:
      - ../:/workdir
     working_dir: /workdir
